{
  "disableRecipebook": {
    // Disables the recipe book entirely, removes the button on client side and removes synced data on server side eliminating its performance impact.
    // (Could call it removed fully): 
    // Default:false
    "disableRecipebook": false
  },

  "enableBetterRecipebookSorting": {
    // Enables better sorting for the list of displayed recipes in the recipebook, craftables and recently used recipes are priotized: 
    // Default:true
    "enableBetterRecipebookSorting": true
  },

  "recipebookShowAll": {
    // Enables the recipe book to show all recipes from the get go, also enables modded recipes to show and disables network/login overhead: 
    // Default:true
    "recipebookShowAll": true
  },

  "fastItemComparisons": {
    // Enables faster item comparison for better performance, disable on mod conflicts: 
    // Default:true
    "fastItemComparisons": true
  },

  "smallerRecipePacket": {
    // Reduces the size of the recipe packet, to prevent errors on too large packets and helps bad connections,
    // requires the mod to be present on both client and server, disable on mod conflicts: 
    // Default:false
    "smallerRecipePacket": false
  },

  "cacheRecipes": {
    // Caches recipe lookups to greatly improve lookup speed: 
    // Default:true
    "cacheRecipes": true
  },

  "logCachingErrors": {
    // Enables error logging for recipe caching: 
    // Default:false
    "logCachingErrors": false
  }
}
